<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Tambahin permission internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InfoIn">
        <activity
            android:name=".ui.auth.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.NewsListActivity"
            android:exported="false" />
        <activity
            android:name=".DetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.FillProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.SelectNewsSourceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.SelectTopicsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.SelectCountryActivity"
            android:exported="false" /> <!-- LoginActivity jadi launcher -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Register -->
        <activity android:name=".ui.auth.RegisterActivity" /> <!-- MainActivity (bukan launcher) -->
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.auth.SettingsActivity" />

    </application>

</manifest>